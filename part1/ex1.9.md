```shell script
   docker run --name port_ex -p 5000:80 devopsdockeruh/ports_exercise
# navigate to http://localhost:5000 in host machine
    curl http://localhost:5000
# return Ports configured correctly!!%
```